import 'package:flutter/material.dart';

class PremiumPlanCard extends StatelessWidget {
  const PremiumPlanCard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
